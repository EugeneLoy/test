name: Test

on:
  workflow_dispatch:
      
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
        
      - name: Test
        env:
          TEST_SECRET: ${{ secrets.TEST_SECRET }}
        shell: bash
        run: |
          echo $TEST_SECRET | base64
